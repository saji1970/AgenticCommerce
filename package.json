{
  "name": "agentic-commerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter mobile start\"",
    "dev:backend": "pnpm --filter backend dev",
    "dev:mobile": "pnpm --filter mobile start",
    "build": "pnpm --filter shared-types build && pnpm --filter validation build && pnpm --filter backend build",
    "build:shared": "pnpm --filter shared-types build && pnpm --filter validation build",
    "type-check": "pnpm -r type-check",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
