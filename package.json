{
  "name": "agentic-commerce",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace @agentic-commerce/backend dev\" \"yarn workspace @agentic-commerce/mobile start\"",
    "dev:backend": "yarn workspace @agentic-commerce/backend dev",
    "dev:mobile": "yarn workspace @agentic-commerce/mobile start",
    "build": "yarn workspace @agentic-commerce/shared-types build && yarn workspace @agentic-commerce/validation build && yarn workspace @agentic-commerce/backend build",
    "build:shared": "yarn workspace @agentic-commerce/shared-types build && yarn workspace @agentic-commerce/validation build",
    "type-check": "yarn workspaces run type-check",
    "lint": "yarn workspaces run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
